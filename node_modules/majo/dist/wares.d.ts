export declare type Middleware = (context: any) => any;
export default class Wares {
    private middlewares;
    constructor();
    use(middleware: Middleware | Middleware[]): this;
    run(context: any): Promise<any>;
}
